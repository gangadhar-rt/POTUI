import { ObjectPipePipe } from './object-pipe.pipe';

describe('ObjectPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectPipePipe();
    expect(pipe).toBeTruthy();
  });
});
