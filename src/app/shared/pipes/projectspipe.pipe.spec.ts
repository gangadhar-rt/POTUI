import { ProjectspipePipe } from './projectspipe.pipe';

describe('ProjectspipePipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectspipePipe();
    expect(pipe).toBeTruthy();
  });
});
